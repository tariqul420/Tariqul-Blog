const loading = () => {
    return (
        <div className="text-center flex justify-center items-center h-screen">
            Profile is Loading...
        </div>
    );
};

export default loading;